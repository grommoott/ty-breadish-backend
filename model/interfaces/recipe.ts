/** Interface for BDRecipe. Recipe itseft loading as raw html
 *
 * id: Id<Recipe>
 * name: string
 * avgRate: number
 * */
interface IBDRecipe {
    id: string,
    name: string,
    avgRate: number
}

export default IBDRecipe
